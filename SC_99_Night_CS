script_key="uJekJkGbMyAucHiYnBMqsUDYXjXTMpWj";
getgenv().ScriptMode = "Diamond" -- ตัวเปลี่ยนโหมดสคริป Diamond / Class

-- Kaitun Mode Diamond
getgenv().Configs = {
    -- แจ้งเตือน ( Notify )
    WebhookUrl = "",
    LimitDiamonds = 0, -- จำกัดเพชร
    EnableLogRam = false,

    -- การฟาร์ม ( Farm )
    LimitDay = 0, -- จำกัดวันถ้าถึงแล้วให้เริ่มเกมใหม่
    MaxStrongholdLevel = 5, -- ระดับสูงสุดของสตรองโฮลด์ถ้าถึงแล้วให้เริ่มเกมใหม่
    CountStronghold = 0, -- นับสตรองโฮลด์ครบแล้วให้เริ่มเกมใหม่
    ClaimBadges = true, -- รับเหรียญทั้งหมดอัตโนมัติ
    FarmCandy = true, -- ฟาร์มลูกอมอัตโนมัติ

    -- คลาส & ร้านค้า ( Class & Shop )
    BuyClass = {"Cyborg"}, -- ชื้อคลาส
    DoFirstTimeReroll = true, -- รีร้านคลาสครั้งแรกฟรี ถ้าไม่มีคลาสที่ต้องการขาย
    AutoEquipClass = "Cyborg", -- เลือกใส่ Class
    UpgradeClass = true, -- อัพเวลคลาสอัตโนมัติ (ไม่ได้ทำเควส แค่อัพเวลตอนมันพร้อมเท่านั้น)
    HalloweenShop = {"Anything1", "Anything2"}, -- รายการชื้อของร้านฮาโลวีน

    -- Misc
    UI = true, -- เปิด/ปิด UI
    LowGraphics = true, -- โหมดกราฟฟิคต่ำ
    FpsCap = 10, -- จำกัด FPS
}

-- Kaitun Mode Class
getgenv().AutoClass = {
    -- ID Class
    ClassID = 0, -- สามารถใช้ได้ทั้งชื่อและตัวเลข

    -- แจ้งเตือน ( Notify )
    EnableLogRam = false, -- Enable/Disable Log

    -- Misc
    UI = true, -- เปิด/ปิด UI

    -- 2 Players Required Class (Medic, Support, Undead) (ไม่จำเป็น)
    Player1 = "Player1Name", -- Change to the first player's name
    Player2 = "Player2Name", -- Change to the second player's name
}

-- Google Sheet Log
getgenv().GoogleSheetLog = {
    Enable = false, -- เปิดใช้ฟีเจอร์
    WebAppURL = "" -- ใส่ URL ของ Google Apps Script
}
loadstring(game:HttpGet("https://gist.githubusercontent.com/Clehxb/a0512b13f2bcc539e037712e13572d14/raw/Night_Loader"))()
